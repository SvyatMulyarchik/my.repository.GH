apiVersion: v1
kind: Pod
metadata:
  name: ubuntu
  labels:
    app: ubuntu
spec:
  containers:
  - image: ghcr.io/pluhin/busy-box:latest
    command:
      - "sleep"
      - "10"
    imagePullPolicy: IfNotPresent
    name: ubuntu
  restartPolicy: Never
